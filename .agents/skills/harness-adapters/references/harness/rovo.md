# Rovo CLI

Rovo dispatch is disabled.
`bin/fm-spawn.sh` refuses explicit `rovo`, configured `crew-harness=rovo`, and raw commands whose effective executable is `rovo` before task metadata or launch delivery.
Do not select Rovo for launch, recovery, or verification until that restriction is removed.
